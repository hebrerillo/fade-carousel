describe('version', function ()
{
    it('should return the correct carousel version', function ()
    {
        this.carousel = new FFCarousel();
        expect(this.carousel).not.toBe(null);
    });
});