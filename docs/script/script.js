'use strict';

(function ()
{
    let sideCarouselOptions = {
        carousel: '#sideCarousel',
        side: 'right',
        intervalDelay: 4000,
        fadeInDuration: 2000
    };

    new FCarousel(sideCarouselOptions);
})();

